enum ValidateEmailState { none, invalid, notCorrect, duplicate }

enum ValidatePasswordState {
  none,
  invalid,
  notCorrect,
}

enum ValidateOTPState {
  none,
  invalid,
  notCorrect,
}

enum ValidatePhoneState { none, invalid, notCorrect, duplicate }

enum ValidateState { none, invalid }

enum CompareTimeState { none, invalid }

enum ValidateTimeState { none, invalid }
